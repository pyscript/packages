{
    "status": "green",
    "notes": "Great news! The package `argon2-cffi` is [officially supported](https://pyodide.org/en/stable/usage/packages-in-pyodide.html) in the latest Pyodide release used by PyScript.\n\nTo use it in PyScript simply add it to the `packages` section of your TOML configuration like this:\n\n```\npackages = [\"argon2-cffi\" ]\n```\n\nOr if you're using a JSON configuration, like this:\n\n```\n{\n    packages: [\"argon2-cffi\"]\n }\n```\n\nRead more about using packages in PyScript [in our documentation](https://docs.pyscript.net/latest/user-guide/configuration/#packages).\n\nSpecifically, the following versions of the package are available for the following Pyodide releases:\n\nPyodide version: package name (version) (PyScript Version)\n\n* 314.0.2: argon2-cffi (23.1.0)\n* 314.0.1: argon2-cffi (23.1.0)\n* 314.0.0: argon2-cffi (23.1.0)\n* 0.29.4: argon2-cffi (23.1.0)\n* 0.29.3: argon2-cffi (23.1.0) ([PyScript 2026.3.1](https://pyscript.net/releases/2026.3.1/))\n* 0.29.2: argon2-cffi (23.1.0)\n* 0.29.1: argon2-cffi (23.1.0) ([PyScript 2026.1.1](https://pyscript.net/releases/2026.1.1/))\n* 0.29.0: argon2-cffi (23.1.0) ([PyScript 2025.11.1](https://pyscript.net/releases/2025.11.1/))\n* 0.28.3: argon2-cffi (23.1.0)\n* 0.28.2: argon2-cffi (23.1.0)\n* 0.28.1: argon2-cffi (23.1.0) ([PyScript 2025.8.1](https://pyscript.net/releases/2025.8.1/))\n* 0.28.0: argon2-cffi (23.1.0)\n* 0.27.7: argon2-cffi (23.1.0) ([PyScript 2025.7.3](https://pyscript.net/releases/2025.7.3/))\n* 0.27.6: argon2-cffi (23.1.0) ([PyScript 2025.5.1](https://pyscript.net/releases/2025.5.1/))\n* 0.27.5: argon2-cffi (23.1.0)\n* 0.27.4: argon2-cffi (23.1.0)\n* 0.27.3: argon2-cffi (23.1.0) ([PyScript 2025.3.1](https://pyscript.net/releases/2025.3.1/))\n* 0.27.2: argon2-cffi (23.1.0) ([PyScript 2025.2.4](https://pyscript.net/releases/2025.2.4/))\n* 0.27.1: argon2-cffi (23.1.0)\n* 0.27.0: argon2-cffi (23.1.0)",
    "pyodide_versions": {
        "314.0.2": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "314.0.1": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "314.0.0": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.29.4": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.29.3": {
            "package_version": "23.1.0",
            "pyscript_version": "2026.3.1"
        },
        "0.29.2": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.29.1": {
            "package_version": "23.1.0",
            "pyscript_version": "2026.1.1"
        },
        "0.29.0": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.11.1"
        },
        "0.28.3": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.28.2": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.28.1": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.8.1"
        },
        "0.28.0": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.27.7": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.7.3"
        },
        "0.27.6": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.5.1"
        },
        "0.27.5": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.27.4": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.27.3": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.3.1"
        },
        "0.27.2": {
            "package_version": "23.1.0",
            "pyscript_version": "2025.2.4"
        },
        "0.27.1": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        },
        "0.27.0": {
            "package_version": "23.1.0",
            "pyscript_version": "unknown"
        }
    },
    "updated_by": "automated script",
    "updated_at": "2026-07-09T15:25:39.091077+00:00",
    "summary": "Argon2 for Python",
    "examples": [
        {
            "title": "Hash And Verify",
            "config": {
                "packages": [
                    "argon2-cffi"
                ]
            },
            "code": "\"\"\"\nA first look at argon2-cffi: hashing a password and verifying it later.\n\nArgon2 is a modern, memory-hard password hashing algorithm and the winner\nof the Password Hashing Competition. The high-level entry point is\n`PasswordHasher`, which picks sensible defaults for you.\n\nDocs: https://argon2-cffi.readthedocs.io/\n\"\"\"\nfrom IPython.core.display import display, HTML\n\n# Package imports for this example.\nfrom argon2 import PasswordHasher\nfrom argon2.exceptions import VerifyMismatchError\n\n# A PasswordHasher with library-recommended defaults. You can tune\n# memory cost, time cost, and parallelism, but the defaults are a fine\n# starting point for most applications.\nhasher = PasswordHasher()\n\nheading(\"1. Hashing a password\")\nnote(\n    \"Calling <code>hash()</code> returns a self-describing string that \"\n    \"encodes the algorithm, parameters, salt, and digest. You store \"\n    \"this whole string in your user database -- there is no separate \"\n    \"salt column to manage.\"\n)\n\npassword = \"correct horse battery staple\"\nstored_hash = hasher.hash(password)\n\ndisplay(HTML(f\"<pre>{stored_hash}</pre>\"), append=True)\n\nnote(\n    \"Notice the prefix <code>$argon2id$</code> and the parameters \"\n    \"<code>m=</code> (memory in KiB), <code>t=</code> (iterations), \"\n    \"and <code>p=</code> (parallelism). Each call uses a fresh random \"\n    \"salt, so hashing the same password twice produces different output.\"\n)\n\nsecond_hash = hasher.hash(password)\ndisplay(HTML(f\"<pre>{second_hash}</pre>\"), append=True)\n\nheading(\"2. Verifying a login attempt\")\nnote(\n    \"Use <code>verify()</code> to check a candidate password against a \"\n    \"stored hash. It returns <code>True</code> on success and raises \"\n    \"an exception on mismatch -- so your code can't accidentally treat \"\n    \"a falsy return as success.\"\n)\n\nok = hasher.verify(stored_hash, \"correct horse battery staple\")\nnote(f\"Correct password verified: <strong>{ok}</strong>\")\n\ntry:\n    hasher.verify(stored_hash, \"hunter2\")\nexcept VerifyMismatchError as exc:\n    note(f\"Wrong password raises: <code>{type(exc).__name__}</code>\")\n",
            "setup": "\"\"\"\nShim IPython's display API onto PyScript so example code written in a\nJupyter/IPython idiom runs unmodified in the browser.\n\"\"\"\n\nimport sys\nimport types\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\nipython = types.ModuleType(\"IPython\")\ncore = types.ModuleType(\"IPython.core\")\ncore_display = types.ModuleType(\"IPython.core.display\")\ncore_display.display = display\ncore_display.HTML = HTML\nipython.core = core\ncore.display = core_display\nipython.version_info = (9, 0, 2, '')\nipython.get_ipython = lambda: None\nipython.display = core_display\nsys.modules[\"IPython\"] = ipython\nsys.modules[\"IPython.core\"] = core\nsys.modules[\"IPython.core.display\"] = core_display\nsys.modules[\"IPython.display\"] = core_display\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n"
        },
        {
            "title": "Tuning Parameters",
            "config": {
                "packages": [
                    "argon2-cffi"
                ]
            },
            "code": "# ---------------------------------------------------------------------\n# Tuning Argon2's cost parameters and reading what's inside a hash.\n# ---------------------------------------------------------------------\n\nheading(\"Cost parameters: tradeoffs between security and speed\")\nnote(\n    \"Argon2 has three knobs: <code>memory_cost</code> (KiB of RAM per \"\n    \"hash), <code>time_cost</code> (iteration count), and \"\n    \"<code>parallelism</code> (lanes). Higher values make brute-force \"\n    \"attacks more expensive, but also slow down legitimate logins. \"\n    \"Pick values that take a comfortable fraction of a second on your \"\n    \"production hardware.\"\n)\n\npassword = \"correct horse battery staple\"\n\n# A few configurations spanning \"fast\" to \"stronger\". The numbers\n# are illustrative; tune for your own environment. Inside a browser the\n# parallelism must only be set to 1. In regular Python this value can be\n# higher.\nconfigurations = [\n    (\"fast\",     {\"memory_cost\": 8 * 1024,  \"time_cost\": 1, \"parallelism\": 1}),\n    (\"default\",  {\"memory_cost\": 64 * 1024, \"time_cost\": 3, \"parallelism\": 1}),\n    (\"stronger\", {\"memory_cost\": 128 * 1024, \"time_cost\": 4, \"parallelism\": 1}),\n]\n\nrows = [\"<tr><th>Profile</th><th>memory_cost (KiB)</th>\"\n        \"<th>time_cost</th><th>parallelism</th><th>hash time (ms)</th></tr>\"]\n\nfor label, params in configurations:\n    hasher = PasswordHasher(**params)\n    start = time.perf_counter()\n    hasher.hash(password)\n    elapsed_ms = (time.perf_counter() - start) * 1000\n    rows.append(\n        f\"<tr><td>{label}</td>\"\n        f\"<td>{params['memory_cost']}</td>\"\n        f\"<td>{params['time_cost']}</td>\"\n        f\"<td>{params['parallelism']}</td>\"\n        f\"<td>{elapsed_ms:.1f}</td></tr>\"\n    )\n\ndisplay(HTML(\"<table>\" + \"\".join(rows) + \"</table>\"), append=True)\n\nheading(\"Migrating old hashes with check_needs_rehash\")\nnote(\n    \"When you raise your cost parameters, existing hashes in your \"\n    \"database become weaker than your new policy. After a successful \"\n    \"login, call <code>check_needs_rehash()</code>: if it returns \"\n    \"<code>True</code>, re-hash the password and store the new value. \"\n    \"This lets you upgrade silently as users log in.\"\n)\n\n# Simulate a hash created long ago with weak parameters.\nold_hasher = PasswordHasher(memory_cost=8 * 1024, time_cost=1, parallelism=1)\nold_hash = old_hasher.hash(password)\n\n# Today's policy is stronger.\ncurrent_hasher = PasswordHasher(\n    memory_cost=64 * 1024, time_cost=3, parallelism=1,\n)\n\nassert current_hasher.verify(old_hash, password)\nneeds_rehash = current_hasher.check_needs_rehash(old_hash)\nnote(f\"Old hash needs rehash under current policy: <strong>{needs_rehash}</strong>\")\n\nif needs_rehash:\n    upgraded = current_hasher.hash(password)\n    note(\"Upgraded hash (store this in place of the old one):\")\n    display(HTML(f\"<pre>{upgraded}</pre>\"), append=True)\n    note(\n        f\"Still needs rehash? \"\n        f\"<strong>{current_hasher.check_needs_rehash(upgraded)}</strong>\"\n    )\n\nheading(\"Choosing the Argon2 variant\")\nnote(\n    \"Argon2 comes in three variants: <code>Type.I</code> (side-channel \"\n    \"resistant), <code>Type.D</code> (GPU-attack resistant), and \"\n    \"<code>Type.ID</code> (hybrid, the recommended default). You \"\n    \"rarely need to change this, but it's a one-liner if you do.\"\n)\n\nid_hasher = PasswordHasher(type=Type.ID)\nsample = id_hasher.hash(password)\nnote(f\"Variant prefix: <code>{sample.split('$')[1]}</code>\")\n",
            "setup": "\"\"\"Lighter setup for the second example. No IPython shim needed.\"\"\"\nimport time\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n\nfrom argon2 import PasswordHasher, Type\n"
        }
    ]
}