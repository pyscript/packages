{
    "status": "green",
    "notes": "Great news! The package `cryptography` is [officially supported](https://pyodide.org/en/stable/usage/packages-in-pyodide.html) in the latest Pyodide release used by PyScript.\n\nTo use it in PyScript simply add it to the `packages` section of your TOML configuration like this:\n\n```\npackages = [\"cryptography\" ]\n```\n\nOr if you're using a JSON configuration, like this:\n\n```\n{\n    packages: [\"cryptography\"]\n }\n```\n\nRead more about using packages in PyScript [in our documentation](https://docs.pyscript.net/latest/user-guide/configuration/#packages).\n\nSpecifically, the following versions of the package are available for the following Pyodide releases:\n\nPyodide version: package name (version) (PyScript Version)\n\n* 314.0.2: cryptography (47.0.0)\n* 314.0.1: cryptography (47.0.0)\n* 314.0.0: cryptography (47.0.0)\n* 0.29.4: cryptography (46.0.3)\n* 0.29.3: cryptography (46.0.3) ([PyScript 2026.3.1](https://pyscript.net/releases/2026.3.1/))\n* 0.29.2: cryptography (46.0.3)\n* 0.29.1: cryptography (46.0.1) ([PyScript 2026.1.1](https://pyscript.net/releases/2026.1.1/))\n* 0.29.0: cryptography (46.0.1) ([PyScript 2025.11.1](https://pyscript.net/releases/2025.11.1/))\n* 0.28.3: cryptography (45.0.5)\n* 0.28.2: cryptography (45.0.5)\n* 0.28.1: cryptography (45.0.5) ([PyScript 2025.8.1](https://pyscript.net/releases/2025.8.1/))\n* 0.28.0: cryptography (45.0.4)\n* 0.27.7: cryptography (42.0.5) ([PyScript 2025.7.3](https://pyscript.net/releases/2025.7.3/))\n* 0.27.6: cryptography (42.0.5) ([PyScript 2025.5.1](https://pyscript.net/releases/2025.5.1/))\n* 0.27.5: cryptography (42.0.5)\n* 0.27.4: cryptography (42.0.5)\n* 0.27.3: cryptography (42.0.5) ([PyScript 2025.3.1](https://pyscript.net/releases/2025.3.1/))\n* 0.27.2: cryptography (42.0.5) ([PyScript 2025.2.4](https://pyscript.net/releases/2025.2.4/))\n* 0.27.1: cryptography (42.0.5)\n* 0.27.0: cryptography (42.0.5)",
    "pyodide_versions": {
        "314.0.2": {
            "package_version": "47.0.0",
            "pyscript_version": "unknown"
        },
        "314.0.1": {
            "package_version": "47.0.0",
            "pyscript_version": "unknown"
        },
        "314.0.0": {
            "package_version": "47.0.0",
            "pyscript_version": "unknown"
        },
        "0.29.4": {
            "package_version": "46.0.3",
            "pyscript_version": "unknown"
        },
        "0.29.3": {
            "package_version": "46.0.3",
            "pyscript_version": "2026.3.1"
        },
        "0.29.2": {
            "package_version": "46.0.3",
            "pyscript_version": "unknown"
        },
        "0.29.1": {
            "package_version": "46.0.1",
            "pyscript_version": "2026.1.1"
        },
        "0.29.0": {
            "package_version": "46.0.1",
            "pyscript_version": "2025.11.1"
        },
        "0.28.3": {
            "package_version": "45.0.5",
            "pyscript_version": "unknown"
        },
        "0.28.2": {
            "package_version": "45.0.5",
            "pyscript_version": "unknown"
        },
        "0.28.1": {
            "package_version": "45.0.5",
            "pyscript_version": "2025.8.1"
        },
        "0.28.0": {
            "package_version": "45.0.4",
            "pyscript_version": "unknown"
        },
        "0.27.7": {
            "package_version": "42.0.5",
            "pyscript_version": "2025.7.3"
        },
        "0.27.6": {
            "package_version": "42.0.5",
            "pyscript_version": "2025.5.1"
        },
        "0.27.5": {
            "package_version": "42.0.5",
            "pyscript_version": "unknown"
        },
        "0.27.4": {
            "package_version": "42.0.5",
            "pyscript_version": "unknown"
        },
        "0.27.3": {
            "package_version": "42.0.5",
            "pyscript_version": "2025.3.1"
        },
        "0.27.2": {
            "package_version": "42.0.5",
            "pyscript_version": "2025.2.4"
        },
        "0.27.1": {
            "package_version": "42.0.5",
            "pyscript_version": "unknown"
        },
        "0.27.0": {
            "package_version": "42.0.5",
            "pyscript_version": "unknown"
        }
    },
    "updated_by": "automated script",
    "updated_at": "2026-07-09T15:25:43.655297+00:00",
    "summary": "cryptography is a package which provides cryptographic recipes and primitives to Python developers.",
    "examples": [
        {
            "title": "Fernet Symmetric",
            "config": {
                "packages": [
                    "cryptography"
                ]
            },
            "code": "\"\"\"\nA first look at `cryptography`: encrypting a secret note.\n\nFernet is the package's high-level \"just give me safe symmetric\nencryption\" recipe. It uses AES-128 in CBC mode with HMAC-SHA256\nfor authentication, with all the bookkeeping (IVs, padding, MAC\nverification) handled for you.\n\nDocs: https://cryptography.io/en/latest/fernet/\n\"\"\"\nfrom IPython.core.display import display, HTML\n\n# Package imports for this example.\nfrom cryptography.fernet import Fernet, InvalidToken\n\n\nheading(\"1. Encrypting a secret note with Fernet\")\nnote(\n    \"Imagine you want to stash a private journal entry in local \"\n    \"storage. Fernet gives you an authenticated, URL-safe token.\"\n)\n\n# A Fernet key is 32 random bytes, base64-encoded. Generate once,\n# then keep it somewhere safe (a vault, a config file, etc.).\nkey = Fernet.generate_key()\ncipher = Fernet(key)\n\njournal_entry = b\"Dear diary: today I learned about authenticated encryption.\"\ntoken = cipher.encrypt(journal_entry)\n\nnote(\"The generated key (keep this secret!):\")\ndisplay(HTML(f\"<code>{key.decode()}</code>\"), append=True)\n\nnote(\"The encrypted token (safe to store or transmit):\")\ndisplay(HTML(f\"<code style='word-break:break-all'>{token.decode()}</code>\"), append=True)\n\n# Decrypting with the same key recovers the original bytes.\nrecovered = cipher.decrypt(token)\nnote(f\"Decrypted message: <strong>{recovered.decode()}</strong>\")\n\nheading(\"Tampering is detected\", level=3)\nnote(\n    \"Fernet tokens are authenticated. Flip a single character and \"\n    \"decryption raises <code>InvalidToken</code> rather than returning \"\n    \"garbage.\"\n)\n\ntampered = token[:-4] + b\"AAAA\"\ntry:\n    cipher.decrypt(tampered)\nexcept InvalidToken:\n    note(\"Caught <code>InvalidToken</code>: the tampered token was rejected. \u2705\")\n",
            "setup": "\"\"\"Setup for the first cryptography example: IPython shim plus imports.\"\"\"\nimport sys\nimport types\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\nipython = types.ModuleType(\"IPython\")\ncore = types.ModuleType(\"IPython.core\")\ncore_display = types.ModuleType(\"IPython.core.display\")\ncore_display.display = display\ncore_display.HTML = HTML\nipython.core = core\ncore.display = core_display\nipython.version_info = (9, 0, 2, '')\nipython.get_ipython = lambda: None\nipython.display = core_display\nsys.modules[\"IPython\"] = ipython\nsys.modules[\"IPython.core\"] = core\nsys.modules[\"IPython.core.display\"] = core_display\nsys.modules[\"IPython.display\"] = core_display\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n"
        },
        {
            "title": "Password Based Key",
            "config": {
                "packages": [
                    "cryptography"
                ]
            },
            "code": "# ---------------------------------------------------------------------\n# Section 2: Turning a human password into a Fernet key with PBKDF2.\n# ---------------------------------------------------------------------\n\n# Package imports for this example.\nimport os\nimport base64\nfrom cryptography.fernet import Fernet, InvalidToken\nfrom cryptography.hazmat.primitives import hashes\nfrom cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC\n\n\nheading(\"2. Password-based encryption with PBKDF2HMAC\")\nnote(\n    \"Humans pick passwords; Fernet wants 32 random bytes. A key \"\n    \"derivation function (KDF) bridges the gap. PBKDF2 stretches \"\n    \"a password with many hash iterations and a random salt, \"\n    \"making brute-force attacks expensive.\"\n)\n\npassword = b\"correct horse battery staple\"\nsalt = os.urandom(16)  # store alongside the ciphertext; not secret.\n\n# 480,000 iterations is a reasonable default for SHA-256 PBKDF2.\nkdf = PBKDF2HMAC(\n    algorithm=hashes.SHA256(),\n    length=32,\n    salt=salt,\n    iterations=480_000,\n)\nderived_key = base64.urlsafe_b64encode(kdf.derive(password))\n\nnote(\"Salt (random, store with the ciphertext):\")\ndisplay(HTML(f\"<code>{salt.hex()}</code>\"), append=True)\nnote(\"Derived Fernet-compatible key:\")\ndisplay(HTML(f\"<code>{derived_key.decode()}</code>\"), append=True)\n\ncipher = Fernet(derived_key)\ntoken = cipher.encrypt(b\"Treasure buried beneath the old oak tree.\")\nnote(\"Encrypted with the password-derived key:\")\ndisplay(HTML(f\"<code style='word-break:break-all'>{token.decode()}</code>\"), append=True)\n\n# To decrypt later, re-derive the key from the same password and salt.\nkdf_again = PBKDF2HMAC(\n    algorithm=hashes.SHA256(),\n    length=32,\n    salt=salt,\n    iterations=480_000,\n)\nrederived = base64.urlsafe_b64encode(kdf_again.derive(password))\nnote(f\"Decrypted: <strong>{Fernet(rederived).decrypt(token).decode()}</strong>\")\n\n# A wrong password produces a different key, and Fernet refuses to decrypt.\nheading(\"Wrong password is rejected\", level=3)\nwrong_kdf = PBKDF2HMAC(\n    algorithm=hashes.SHA256(), length=32, salt=salt, iterations=480_000,\n)\nwrong_key = base64.urlsafe_b64encode(wrong_kdf.derive(b\"hunter2\"))\ntry:\n    Fernet(wrong_key).decrypt(token)\nexcept InvalidToken:\n    note(\"Caught <code>InvalidToken</code>: wrong password, no plaintext. \u2705\")\n",
            "setup": "\"\"\"Setup for example 2: same names as cell 1, no IPython shim.\"\"\"\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(*args, **kwargs, target=__pyscript_display_target__)\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n"
        },
        {
            "title": "Rsa Signing",
            "config": {
                "packages": [
                    "cryptography"
                ]
            },
            "code": "# ---------------------------------------------------------------------\n# Section 3: Asymmetric crypto: signing a message with RSA.\n# ---------------------------------------------------------------------\n# Package imports for this example.\nfrom cryptography.hazmat.primitives import hashes, serialization\nfrom cryptography.hazmat.primitives.asymmetric import rsa, padding\nfrom cryptography.exceptions import InvalidSignature\n\n\nheading(\"3. RSA: generating keys, signing, and verifying\")\nnote(\n    \"Asymmetric crypto uses a key pair: the <em>private</em> key signs, \"\n    \"the <em>public</em> key verifies. We'll generate a 2048-bit RSA \"\n    \"keypair, sign a release note, and confirm a verifier can detect \"\n    \"tampering.\"\n)\n\n# Generate a fresh RSA keypair. 2048 bits is the modern minimum.\nprivate_key = rsa.generate_private_key(public_exponent=65537, key_size=2048)\npublic_key = private_key.public_key()\n\n# Serialize the public key to PEM so it can be shared.\npublic_pem = public_key.public_bytes(\n    encoding=serialization.Encoding.PEM,\n    format=serialization.PublicFormat.SubjectPublicKeyInfo,\n)\nnote(\"Public key (PEM, safe to share):\")\ndisplay(HTML(f\"<pre style='font-size:0.85em'>{public_pem.decode()}</pre>\"), append=True)\n\nmessage = b\"Release v1.4.2 ships at midnight. Approved by Alice.\"\n\n# Sign with PSS padding and SHA-256, the modern recommendation.\nsignature = private_key.sign(\n    message,\n    padding.PSS(\n        mgf=padding.MGF1(hashes.SHA256()),\n        salt_length=padding.PSS.MAX_LENGTH,\n    ),\n    hashes.SHA256(),\n)\nnote(f\"Signature length: <strong>{len(signature)} bytes</strong> (first 16 in hex):\")\ndisplay(HTML(f\"<code>{signature[:16].hex()}\u2026</code>\"), append=True)\n\n# A verifier loads the public key from PEM and checks the signature.\nverifier_key = serialization.load_pem_public_key(public_pem)\n\n\ndef verify(msg, sig):\n    try:\n        verifier_key.verify(\n            sig, msg,\n            padding.PSS(\n                mgf=padding.MGF1(hashes.SHA256()),\n                salt_length=padding.PSS.MAX_LENGTH,\n            ),\n            hashes.SHA256(),\n        )\n        return True\n    except InvalidSignature:\n        return False\n\n\nheading(\"Verification\", level=3)\nnote(f\"Original message verifies: <strong>{verify(message, signature)}</strong> \u2705\")\n\ntampered = message.replace(b\"Alice\", b\"Eve  \")\nnote(f\"Tampered message verifies: <strong>{verify(tampered, signature)}</strong> \u274c\")\nnote(\n    \"The signature is bound to the exact bytes that were signed. \"\n    \"Any change \u2014 even swapping a name \u2014 invalidates it.\"\n)\n",
            "setup": "\"\"\"Setup for example 3: same names as cell 1, no IPython shim.\"\"\"\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(*args, **kwargs, target=__pyscript_display_target__)\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n"
        }
    ]
}