{
    "status": "green",
    "notes": "Great news! The package `distro` is [officially supported](https://pyodide.org/en/stable/usage/packages-in-pyodide.html) in the latest Pyodide release used by PyScript.\n\nTo use it in PyScript simply add it to the `packages` section of your TOML configuration like this:\n\n```\npackages = [\"distro\" ]\n```\n\nOr if you're using a JSON configuration, like this:\n\n```\n{\n    packages: [\"distro\"]\n }\n```\n\nRead more about using packages in PyScript [in our documentation](https://docs.pyscript.net/latest/user-guide/configuration/#packages).\n\nSpecifically, the following versions of the package are available for the following Pyodide releases:\n\nPyodide version: package name (version) (PyScript Version)\n\n* 314.0.2: distro (1.9.0)\n* 314.0.1: distro (1.9.0)\n* 314.0.0: distro (1.9.0)\n* 0.29.4: distro (1.9.0)\n* 0.29.3: distro (1.9.0) ([PyScript 2026.3.1](https://pyscript.net/releases/2026.3.1/))\n* 0.29.2: distro (1.9.0)\n* 0.29.1: distro (1.9.0) ([PyScript 2026.1.1](https://pyscript.net/releases/2026.1.1/))\n* 0.29.0: distro (1.9.0) ([PyScript 2025.11.1](https://pyscript.net/releases/2025.11.1/))\n* 0.28.3: distro (1.9.0)\n* 0.28.2: distro (1.9.0)\n* 0.28.1: distro (1.9.0) ([PyScript 2025.8.1](https://pyscript.net/releases/2025.8.1/))\n* 0.28.0: distro (1.9.0)\n* 0.27.7: distro (1.9.0) ([PyScript 2025.7.3](https://pyscript.net/releases/2025.7.3/))\n* 0.27.6: distro (1.9.0) ([PyScript 2025.5.1](https://pyscript.net/releases/2025.5.1/))\n* 0.27.5: distro (1.9.0)",
    "pyodide_versions": {
        "314.0.2": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "314.0.1": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "314.0.0": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.29.4": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.29.3": {
            "package_version": "1.9.0",
            "pyscript_version": "2026.3.1"
        },
        "0.29.2": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.29.1": {
            "package_version": "1.9.0",
            "pyscript_version": "2026.1.1"
        },
        "0.29.0": {
            "package_version": "1.9.0",
            "pyscript_version": "2025.11.1"
        },
        "0.28.3": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.28.2": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.28.1": {
            "package_version": "1.9.0",
            "pyscript_version": "2025.8.1"
        },
        "0.28.0": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        },
        "0.27.7": {
            "package_version": "1.9.0",
            "pyscript_version": "2025.7.3"
        },
        "0.27.6": {
            "package_version": "1.9.0",
            "pyscript_version": "2025.5.1"
        },
        "0.27.5": {
            "package_version": "1.9.0",
            "pyscript_version": "unknown"
        }
    },
    "updated_by": "automated script",
    "updated_at": "2026-07-09T15:25:44.753839+00:00",
    "summary": "Distro - an OS platform information API",
    "examples": [
        {
            "title": "Distro Overview",
            "config": {
                "packages": [
                    "distro"
                ]
            },
            "code": "\"\"\"\nA first look at `distro`: querying basic OS distribution info.\n\n`distro` reads OS-release files (and similar sources) to give you a\nreliable, machine-readable picture of the system Python is running on.\nIt's the modern replacement for the removed `platform.linux_distribution`.\n\nDocs: https://distro.readthedocs.io/\n\"\"\"\nfrom IPython.core.display import display, HTML\n# Package imports for this example.\nimport distro\n\n\nheading(\"Who am I running on?\")\nnote(\n    \"The most common starting point: a friendly name, a stable ID, \"\n    \"and a version string.\"\n)\n\n# `name(pretty=True)` returns a human-readable label like\n# \"Ubuntu 22.04.3 LTS (Jammy Jellyfish)\". Without `pretty`, it returns\n# just the distribution's name.\ndisplay(HTML(\n    f\"<ul>\"\n    f\"<li><b>Pretty name:</b> {distro.name(pretty=True) or '(unknown)'}</li>\"\n    f\"<li><b>Plain name:</b> {distro.name() or '(unknown)'}</li>\"\n    f\"<li><b>ID:</b> <code>{distro.id() or '(unknown)'}</code></li>\"\n    f\"<li><b>Version:</b> {distro.version(best=True) or '(unknown)'}</li>\"\n    f\"<li><b>Codename:</b> {distro.codename() or '(none)'}</li>\"\n    f\"</ul>\"\n), append=True)\n\nnote(\n    \"Inside the Pyodide sandbox there's no traditional Linux \"\n    \"distribution to detect, so several of these fields may come \"\n    \"back empty. On a real Linux host you would see values like \"\n    \"<code>id() == 'ubuntu'</code> or <code>id() == 'fedora'</code>.\"\n)\n",
            "setup": "\"\"\"\nShim IPython's display API onto PyScript so example code written in a\nJupyter/IPython idiom runs unmodified in the browser.\n\"\"\"\n\nimport sys\nimport types\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    \"\"\"Wrap pyscript.display so output lands in the example target.\"\"\"\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\nipython = types.ModuleType(\"IPython\")\ncore = types.ModuleType(\"IPython.core\")\ncore_display = types.ModuleType(\"IPython.core.display\")\ncore_display.display = display\ncore_display.HTML = HTML\nipython.core = core\ncore.display = core_display\nipython.version_info = (9, 0, 2, '')\nipython.get_ipython = lambda: None\nipython.display = core_display\nsys.modules[\"IPython\"] = ipython\nsys.modules[\"IPython.core\"] = core\nsys.modules[\"IPython.core.display\"] = core_display\nsys.modules[\"IPython.display\"] = core_display\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n"
        },
        {
            "title": "Structured Info",
            "config": {
                "packages": [
                    "distro"
                ]
            },
            "code": "# ---------------------------------------------------------------------\n# Structured access: info() dict and split version parts.\n# ---------------------------------------------------------------------\n\nheading(\"Structured distribution info\")\nnote(\n    \"<code>distro.info()</code> bundles everything into a single \"\n    \"dictionary, which is handy when you want to log or serialize \"\n    \"the OS context (e.g. as part of a bug report or telemetry).\"\n)\n\ninfo = distro.info()\ndisplay(info, append=True)\n\nheading(\"Splitting the version into parts\")\nnote(\n    \"Version strings are awkward to compare as text. \"\n    \"<code>version_parts()</code> returns a \"\n    \"<code>(major, minor, build_number)</code> tuple, and you can \"\n    \"also ask for each piece individually.\"\n)\n\nmajor, minor, build = distro.version_parts(best=True)\nrows = [\n    (\"major\", major),\n    (\"minor\", minor),\n    (\"build_number\", build),\n    (\"major() helper\", distro.major_version(best=True)),\n    (\"minor() helper\", distro.minor_version(best=True)),\n    (\"build_number() helper\", distro.build_number(best=True)),\n]\ntable = \"<table border='1' cellpadding='4'><tr><th>Field</th><th>Value</th></tr>\"\nfor label, value in rows:\n    table += f\"<tr><td>{label}</td><td><code>{value!r}</code></code></tr>\"\ntable += \"</table>\"\ndisplay(HTML(table), append=True)\n\nnote(\n    \"Passing <code>best=True</code> asks distro to consult every \"\n    \"available data source and return the most precise version it \"\n    \"can find, instead of stopping at the first match.\"\n)\n",
            "setup": "\"\"\"Lighter setup for later examples: same names, no IPython shim.\"\"\"\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n\nimport distro\n"
        },
        {
            "title": "Os Release And Like",
            "config": {
                "packages": [
                    "distro"
                ]
            },
            "code": "# ---------------------------------------------------------------------\n# Branching on the distribution family using `id()` and `like()`.\n# ---------------------------------------------------------------------\n\nheading(\"Raw os-release contents\")\nnote(\n    \"<code>distro.os_release_info()</code> returns the parsed \"\n    \"key-value pairs from <code>/etc/os-release</code>. This is \"\n    \"useful when you need a field that doesn't have a dedicated \"\n    \"helper (for example, <code>VARIANT</code> or <code>BUG_REPORT_URL</code>).\"\n)\n\nos_release = distro.os_release_info()\ndisplay(os_release or {\"(no os-release available in this environment)\": \"\"},\n        append=True)\n\n\nheading(\"Picking a code path by distribution family\")\nnote(\n    \"Many tools install differently on Debian-likes vs. Red Hat-likes. \"\n    \"<code>distro.id()</code> gives the specific distribution, while \"\n    \"<code>distro.like()</code> returns a space-separated list of \"\n    \"parent families (e.g. <code>'debian'</code> for Ubuntu, or \"\n    \"<code>'rhel fedora'</code> for CentOS Stream).\"\n)\n\n\ndef install_command_for(distro_id, distro_like):\n    \"\"\"Pick a plausible package install command for a given distro.\"\"\"\n    family = f\"{distro_id} {distro_like}\".split()\n    if \"debian\" in family or \"ubuntu\" in family:\n        return \"sudo apt-get install <package>\"\n    if \"rhel\" in family or \"fedora\" in family or \"centos\" in family:\n        return \"sudo dnf install <package>\"\n    if \"arch\" in family:\n        return \"sudo pacman -S <package>\"\n    if \"alpine\" in family:\n        return \"sudo apk add <package>\"\n    return \"(no known package manager for this system)\"\n\n\n# Demonstrate the logic on a few representative distributions, since\n# the sandbox itself won't report as any of them.\nsamples = [\n    (\"ubuntu\", \"debian\"),\n    (\"debian\", \"\"),\n    (\"centos\", \"rhel fedora\"),\n    (\"fedora\", \"\"),\n    (\"manjaro\", \"arch\"),\n    (\"alpine\", \"\"),\n]\n\nrows = \"<tr><th>id</th><th>like</th><th>install command</th></tr>\"\nfor sample_id, sample_like in samples:\n    cmd = install_command_for(sample_id, sample_like)\n    rows += (\n        f\"<tr><td><code>{sample_id}</code></td>\"\n        f\"<td><code>{sample_like or '-'}</code></td>\"\n        f\"<td><code>{cmd}</code></td></tr>\"\n    )\ndisplay(HTML(f\"<table border='1' cellpadding='4'>{rows}</table>\"), append=True)\n\nnote(\n    f\"On the actual host, <code>distro.id()</code> is \"\n    f\"<code>{distro.id() or '(empty)'}</code> and \"\n    f\"<code>distro.like()</code> is \"\n    f\"<code>{distro.like() or '(empty)'}</code>, so \"\n    f\"<code>install_command_for(...)</code> would suggest: \"\n    f\"<code>{install_command_for(distro.id(), distro.like())}</code>.\"\n)\n",
            "setup": "\"\"\"Lighter setup for later examples: same names, no IPython shim.\"\"\"\nimport js\nfrom pyscript import window, HTML, display as _display\n\njs.alert = window.alert\n\n\ndef display(*args, **kwargs):\n    return _display(\n        *args, **kwargs, target=__pyscript_display_target__,\n    )\n\n\ndef heading(text, level=2):\n    display(HTML(f\"<h{level}>{text}</h{level}>\"), append=True)\n\n\ndef note(text):\n    display(HTML(f\"<p>{text}</p>\"), append=True)\n\n\nimport distro\n"
        }
    ]
}